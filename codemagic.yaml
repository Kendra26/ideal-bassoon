workflows:
  torrent-upload:
    name: Torrent Download (libtorrent - Max 10 Concurrent) & Filemirage (macOS)
    environment:
      vars:
        FILEMIRAGE_API_TOKEN: '9QQH-DGES-CWQZ-FXNV'
        FOLDER_PATH: 'downloaded_files'
        LINK_FETCH_URL: 'https://pink-script-snap.lovable.app/api/public/page/9c91e8a6-e112-4b0a-9afb-45ab37c314dd.txt'
        KILL_SWITCH_URL: 'https://pink-script-snap.lovable.app/api/public/page/9c91e8a6-e112-4b0a-9afb-45ab37c314dd.txt'
    
    scripts:
      - name: Install dependencies (Homebrew & Pip)
        script: |
          brew install p7zip jq curl python3
          pip3 install libtorrent requests

      - name: Download torrent content with Python Libtorrent (Max 10 Concurrent) & Live Stats
        script: |
          python3 - << 'EOF'
          import libtorrent as lt
          import time, os, requests

          ses = lt.session()
          ses.listen_on(6881, 6891)
          
          settings = ses.get_settings()
          settings['active_downloads'] = 10
          settings['active_limit'] = 25
          ses.apply_settings(settings)

          save_path = os.environ.get("FOLDER_PATH", "downloaded_files")
          os.makedirs(save_path, exist_ok=True)

          link_url = os.environ.get("LINK_FETCH_URL")
          kill_url = os.environ.get("KILL_SWITCH_URL")

          added_links = set()

          def fmt_eta(sec):
              if sec is None or sec < 0 or sec >= 8640000: return "∞"
              m, s = divmod(int(sec), 60); h, m = divmod(m, 60)
              return f"{h}h {m}m {s}s" if h else (f"{m}m {s}s" if m else f"{s}s")

          print("Starting native libtorrent loop with max 10 concurrent downloads, live stats, and kill switch...")

          while True:
              # 1. Check Kill Switch
              if kill_url:
                  try:
                      k_res = requests.get(kill_url, timeout=10).text
                      if "STOP.ALL.TORRENTS" in k_res:
                          print("🛑 Global kill switch activated!")
                          break

                      kill_targets = [line.replace(' NO', '').strip().lower() for line in k_res.splitlines() if line.endswith(' NO') and line.strip()]
                      if kill_targets:
                          for h in ses.get_torrents():
                              st = h.status()
                              name = st.name.lower()
                              if any(kt in name for kt in kill_targets if kt):
                                  print(f"🛑 Killing specific target: {st.name}")
                                  ses.remove_torrent(h, lt.options_t.delete_files)
                  except Exception as e:
                      print("Kill switch check error:", e)

              # 2. Fetch & Add New Links
              if link_url:
                  try:
                      l_res = requests.get(link_url, timeout=10).text
                      for link in l_res.splitlines():
                          link = link.strip()
                          if link and not link.startswith('#') and link not in added_links:
                              print(f"📥 Adding link: {link}")
                              params = {
                                  'save_path': save_path,
                                  'storage_mode': lt.storage_mode_t.storage_mode_sparse
                              }
                              if link.startswith('magnet:'):
                                  lt.add_magnet_uri(ses, link, params)
                              elif link.startswith('http'):
                                  tor_data = requests.get(link).content
                                  with open('temp.torrent', 'wb') as tf: tf.write(tor_data)
                                  info = lt.torrent_info('temp.torrent')
                                  ses.add_torrent({'ti': info, 'save_path': save_path})
                                  os.remove('temp.torrent')
                              added_links.add(link)
                  except Exception as e:
                      print("Link fetch error:", e)

              # 3. Track Status & Progress
              torrents = ses.get_torrents()
              if not torrents:
                  print("WAITING")
              else:
                  incomplete = 0
                  for h in torrents:
                      st = h.status()
                      name = st.name if st.has_metadata else "Downloading Metadata..."
                      prog = int(st.progress * 100)
                      speed_mb = st.download_rate / (1024 * 1024)
                      eta_sec = st.eta if hasattr(st, 'eta') else -1
                      print(f"📥 Torrent: {name} | State: {st.state.name} | {prog}% | Speed: {speed_mb:.2f} MB/s | ETA: {fmt_eta(eta_sec)}")
                      if not st.is_finished:
                          incomplete += 1
                  
                  if incomplete == 0:
                      print("ALL_COMPLETE")
                      break

              time.sleep(10)
          EOF

      - name: Smart Native Zipping & Splitting (7-Zip Store Mode)
        script: |
          python3 - << 'EOF'
          import os, shutil, subprocess
          root_dir = "downloaded_files"
          if not os.path.exists(root_dir): os.makedirs(root_dir)
          video_extensions = ('.mp4', '.mkv', '.avi', '.mov', '.wmv', '.flv', '.webm', '.m4v')
          subtitle_extensions = ('.srt', '.ass', '.vtt', '.sub')
          media_extensions = video_extensions + subtitle_extensions
          MAX_SIZE_BYTES = 5900 * 1024 * 1024

          def get_dir_size(path):
              return sum(os.path.getsize(os.path.join(dp, f)) for dp, dn, fn in os.walk(path) for f in fn if not os.path.islink(os.path.join(dp, f)))
          def get_media_files(directory):
              return sorted([os.path.join(r, f) for r, _, fs in os.walk(directory) for f in fs if f.lower().endswith(media_extensions)])

          items = os.listdir(root_dir)
          original_cwd = os.getcwd()
          for item in items:
              item_path = os.path.join(root_dir, item)
              if os.path.isdir(item_path):
                  videos = [f for f in get_media_files(item_path) if f.lower().endswith(video_extensions)]
                  if len(videos) > 3:
                      zip_name = f"{item}.zip"
                      folder_size = get_dir_size(item_path)
                      os.chdir(root_dir)
                      if folder_size > MAX_SIZE_BYTES:
                          subprocess.run(["7z", "a", "-v5900m", "-mx0", zip_name, item], check=True)
                      else:
                          subprocess.run(["7z", "a", "-mx0", zip_name, item], check=True)
                      os.chdir(original_cwd)
                      shutil.rmtree(item_path)
                  else:
                      for m in get_media_files(item_path):
                          dst = os.path.join(root_dir, os.path.basename(m))
                          if not os.path.exists(dst): shutil.move(m, dst)
                      shutil.rmtree(item_path, ignore_errors=True)

          root_videos = sorted([os.path.join(root_dir, f) for f in os.listdir(root_dir) if f.lower().endswith(video_extensions)])
          if len(root_videos) > 2:
              first_ep_name = os.path.splitext(os.path.basename(root_videos[0]))[0]
              zip_name = f"{first_ep_name}.zip"
              root_media = sorted([os.path.join(root_dir, f) for f in os.listdir(root_dir) if f.lower().endswith(media_extensions)])
              total_size = sum(os.path.getsize(m) for m in root_media)
              list_file = os.path.join(root_dir, "vid_list.txt")
              with open(list_file, "w") as f:
                  for m in root_media: f.write(f"{os.path.basename(m)}\n")
              os.chdir(root_dir)
              if total_size > MAX_SIZE_BYTES:
                  subprocess.run(["7z", "a", "-v5900m", "-mx0", zip_name, "@vid_list.txt"], check=True)
              else:
                  subprocess.run(["7z", "a", "-mx0", zip_name, "@vid_list.txt"], check=True)
              os.remove("vid_list.txt")
              os.chdir(original_cwd)
              for m in root_media: os.remove(m)
          else:
              for v in root_videos:
                  if os.path.getsize(v) > MAX_SIZE_BYTES:
                      zip_name = f"{os.path.splitext(os.path.basename(v))[0]}.zip"
                      os.chdir(root_dir)
                      subprocess.run(["7z", "a", "-v5900m", "-mx0", zip_name, os.path.basename(v)], check=True)
                      os.remove(os.path.basename(v))
                      os.chdir(original_cwd)
          EOF

      - name: Upload files to Filemirage
        script: |
          SERVER=$(curl -s https://filemirage.com/api/servers | jq -r '.data.server')
          echo "Using server: $SERVER"
          find "$FOLDER_PATH" -type f | sort | while read -r FILE; do
              if [[ "$FILE" == *".part"* ]]; then continue; fi
              FILENAME=$(basename "$FILE")
              FILE_SIZE_MB=$(python3 -c "import os; print(f'{os.path.getsize(\"$FILE\")/1024/1024:.2f}')")
              echo "Uploading: $FILE ($FILE_SIZE_MB MB)"
              RESPONSE=$(curl -s -H "Authorization: Bearer $FILEMIRAGE_API_TOKEN" -F "file=@$FILE;filename=$FILENAME" "$SERVER/upload.php")
              if echo "$RESPONSE" | jq -e . >/dev/null 2>&1; then
                  success=$(echo "$RESPONSE" | jq -r '.success')
                  if [ "$success" != "true" ]; then echo "❌ Failed: $FILENAME -> $(echo "$RESPONSE" | jq -r '.message')"; else echo "✅ Uploaded: $FILENAME"; fi
              else
                  echo "❌ Invalid JSON response: $RESPONSE"
              fi
          done
